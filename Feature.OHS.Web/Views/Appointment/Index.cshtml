@model Feature.OHS.Web.ViewModels.AppointmentViewModel

@*<link href='../fullcalendar.min.css' rel='stylesheet' />
    <link href='../fullcalendar.print.min.css' rel='stylesheet' media='print' />
    <script src='../lib/moment.min.js'></script>
    <script src='../lib/jquery.min.js'></script>
    <script src='../fullcalendar.min.js'></script>*@

<style>

    #calendar {
        max-width: 800px;
        margin: 0 auto;
    }
</style>
<div class="container">
    <div class="row">
        <div class="col-sm-6" id='calendar'></div>

        <form asp-action="Create" asp-controller="Appointment" method="post" id="add-appointment">
            <fieldset>
                <div class="panel panel-default">
                    <div class="panel-heading">Patient Information </div>
                    <div class="panel-body">

                        <div class="form-group form-inline">
                            <a id="patientSearchId" href="javascript:void(0)">  <i class="fa fa-search" aria-hidden="true"></i></a>
                            <input asp-for="IdNumber" class="form-control form-control-sm ml-3 w-75" placeholder="Search Patient" aria-label="Search">
                        </div>

                        <div class="col-sm-4 ">

                            <div class="form-group form-inline">
                                <label for="firstName">Name <span>*</span></label>
                                @if (Model?.FirstName == null)

                                {
                                    <input asp-for="FirstName" type="text" class="form-control" placeholder="">
                                    <span asp-validation-for="FirstName" class="text-danger"></span>
                                }
                                else
                                {
                                    <input asp-for="FirstName" type="text" value=@Model.FirstName class="form-control">
                                    <span asp-validation-for="FirstName" class="text-danger"></span>
                                }
                            </div>
                            <div class="form-group form-inline">
                                @if (Model?.LastName == null)
                                {
                                    <label for="Surname">Surname <span>*</span></label>
                                    <input asp-for="LastName" type="text" class="form-control" placeholder="">
                                    <span asp-validation-for="LastName" class="text-danger"></span>
                                }
                                else
                                {
                                    <label for="Surname">Surname <span>*</span></label>
                                    <input asp-for="LastName" type="text" class="form-control" value="@Model.LastName">
                                    <span asp-validation-for="LastName" class="text-danger"></span>
                                }
                            </div>
                            <div class="form-group form-inline">
                                <label for="IdNumber">Id Number <span>*</span></label>
                                <input asp-for="IdNumber" type="text" class="form-control" placeholder="">
                                <span asp-validation-for="LastName" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="col-sm-4">
                            <div class="form-group form-inline">
                                <label for="ContactNumber">Contact Number</span></label>
                                <input type="text" class="form-control" placeholder="">
                                <span class="text-danger"></span>
                            </div>

                            <div class="form-group form-inline">
                                <label for="EmailAddress">Email Address <span>*</span></label>
                                <input type="text" class="form-control" placeholder="">
                                <span class="text-danger"></span>
                            </div>
                            <div class="form-group form-inline">
                                <label for="ReminderType">Reminder Type <span>*</span></label>
                                <select name="ReminderType" class="form-control" placeholder="">
                                    <option>-----None-----</option>
                                    <option value="0">SMS</option>
                                    <option value="1">Email</option>
                                </select>
                                <span class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </fieldset>
            <fieldset>
                <div class="panel panel-default">
                    <div class="panel-heading">Appointment Information </div>
                    <div class="panel-body">

                        <div class="col-sm-4 ">
                            <div class="form-group form-inline">
                                <label for="Title">Appointment Subject <span></span></label>
                                <input asp-for="Title" type="text" class="form-control" placeholder="">
                                <span asp-validation-for="Title" class="text-danger"></span>
                            </div>
                            <div class="form-group form-inline">
                                <label for="description">Description:</label>
                                <textarea class="form-control" rows="2"></textarea>
                            </div>
                        </div>
                        <div class="col-sm-3">
                            <div class="form-group form-inline">
                                <label for="Date">Date<span></span></label>
                                <input asp-for="Start" type="date" class="form-control" placeholder="">
                                <span asp-validation-for="Start" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="col-sm-3">
                            <div class="form-group form-inline">
                                <label for="Date">Time<span></span></label>
                                <input asp-for="Time" type="time" class="form-control" placeholder="">
                                <span asp-validation-for="Time" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </fieldset>
            <div class="col-sm-6">
            </div>
            <div class="col-sm-6">
                <input id="submit" class="hs-button primary large action-button next" type="submit" value="Submit">
            </div>
        </form>
    </div>
</div>
<div id="appdiv"></div>
<script>

    $(document).ready(function () {

        var appointments = [];
        $.ajax({
            type: "Get",
            url: "/Appointment/GetAppointments",
            success: function (data) {
                //console.log(data);

                $.each(data, function (i, v) {
                    //console.log(v);

                    appointments.push({
                        id: v.AppointmentId,
                        title: v.Title,
                        description: v.Description,
                        start: v.Start,//.format('YYYY-MM-DD'),
                        end: v.End,//v.End != null ? moment(v.End).format('YYYY-MM-DD') : null,
                        allDay: false
                    });
                });

                console.log(appointments);

                //  GenerateCalender(appointments);
            },
            error: function (error) {
                alert('failed')

            }
        }

        );

        $("#patientSearchId").on("click", function (e) {
           // e.preventDefault();

            var idNumber = $("#IdNumber").val();

            var button = $(this);

            button.attr("href", "/Appointment/GetAppointment?id=" + idNumber);
            button.click();

            //$("#IdNumber").on('keyup', function (e) { 
            //    var idNumber = $(this).val();

            //    //patientSearchId



            //});


            //$.ajax({
            //    type: "POST",
            //    url: "/Appointment/GetAppointment",
            //    data: { id: idNumber },
            //    success: function (data) {
            //        //console.log(data);
                   
            //        $.each(data, function (i, v) {
            //            //console.log(v);

            //            appointments.push({
            //                id: v.AppointmentId,
            //                title: v.Title,
            //                description: v.Description,
            //                start: v.Start,//.format('YYYY-MM-DD'),
            //                end: v.End,//v.End != null ? moment(v.End).format('YYYY-MM-DD') : null,
            //                allDay: false
            //            });
            //        });

            //        console.log(appointments);

            //        //  GenerateCalender(appointments);
            //    },
            //    error: function (error) {
            //        alert('failed')
            //        console.log(error);
            //    }
            //});
        });

        // function GenerateCalender(appointments) {
        // $('calender').fullCalendar('destroy');
        $('#calendar').fullCalendar({
            header: {
                left: 'prev,next today',
                center: 'title',
                right: 'month,basicWeek,basicDay,agenda'
            },
            defaultDate: new Date(),
            navLinks: true, // can click day/week names to navigate views
            editable: true,
            eventLimit: true, // allow "more" link when too many events
            eventColor: '#00bfff'
            , events: "/Appointment/GetAppointments",
            eventClick: function (calEvent, jsEvent, view) {
                $('#appointmentModal');
                var $description = $('#appdiv').html('content');
                $description.append($('<p />').html('<b>Start:</b>' + calEvent.start));
                $description.append($('<p />').html('<b>End:</b>' + calEvent.end));
                $description.append($('<p />').html('<b>Description:</b>' + calEvent.description));
                $('#appointmentModal #appointmentDetails').empty().html($description);
                $('#appointmentModal').modal();
            },
            eventDrop: function (event, delta, revertFunc, jsEvent, ui, view) {
                $('#appointmentModal');
                var $description = $('#appdiv').html('content');
                $description.append($('<p />').html('<b>Start:</b>' + event.start));
                $description.append($('<p />').html('<b>End:</b>' + event.end));
                $description.append($('<p />').html('<b>Description:</b>' + event.description));
                $('#appointmentModal #appointmentDetails').empty().html($description);
                var aDate = new Date(
                    Date.parse(event.start)
                );
                var timeString = "";
                $("#start").val(aDate);
                var h = aDate.getHours();
                var m = aDate.getMinutes();
                var s = aDate.getSeconds();
                if (h < 10) h = '0' + h;
                if (m < 10) m = '0' + m;
                if (s < 10) s = '0' + s;
                timeString = h + ':' + m + ':' + s;
                $('#time').val(timeString);
                $("#title").val(event.title);
                $("#desc").val(event.description);
                $('#appointmentModal').modal();
            }

        })
        

    });
</script>
<!-- Button trigger modal -->
<!-- Modal -->
<div class="modal fade" id="appointmentModal" tabindex="-1" role="dialog" aria-labelledby="appointmentModalLabel" aria-hidden="true">
    <div class="modal-dialog" style="display:grid;height:60%; width:50%;" role="document">
        <div class="modal-content" style="width:100%">
            <div class="modal-header">
                <a class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></a>
                <h5 class="modal-title" id="appointmentModalLabel">Doctor Appointment</h5>
            </div>

            <form asp-action="UpdateAppointment" asp-controller="Appointment" method="post">
                <div class="modal-body" style="height:70%; width:100%;">

                    <div class=" col-sm-12">
                        <input asp-for="AppointmentId" type="hidden" />
                        <input asp-for="IdNumber" type="hidden" />
                        <label for="Date">Date <span>*</span></label>
                        <input asp-for="Start" id="start" type="text" class="form-control" placeholder="">
                        <label for="Time">Time <span>*</span></label>
                        <input asp-for="Time" id="time" type="time" class="form-control" placeholder="">
                        <label for="Title">Subject <span>*</span></label>
                        <input asp-for="Title" id="title" type="text" class="form-control" placeholder="">
                        <label for="Description">Description <span>*</span></label>
                        <textarea id="desc" class="form-control" rows="2"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>